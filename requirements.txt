# Pinned dependency versions — generated from pip freeze on 2026-03-05.
# To update: deploy to Railway, run `pip freeze > requirements.txt` there,
# commit the result. Never use unpinned ranges in production.
#
# gevent and python-pptx are production-only (not installable on Windows dev).
# Versions below are the latest stable releases verified against this codebase.
# Re-pin from Railway after first successful deploy.

Django==5.1
djangorestframework==3.15.0
boto3==1.42.44
PyMuPDF==1.27.1
openai==2.20.0
python-dotenv==1.2.1
requests==2.32.3
reportlab==4.4.10
python-docx==1.2.0
gunicorn==23.0.0
gevent==24.2.1
whitenoise==6.8.2
psycopg2-binary==2.9.11
dj-database-url==2.1.0
django-cors-headers==4.6.0
python-pptx==1.0.2
